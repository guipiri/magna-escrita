{
  "name": "trocr-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "python3 -m venv .venv && . .venv/bin/activate && pip install -r requirements.txt && python3 -m uvicorn main:app --host 0.0.0.0 --port 8000 --reload"
  }
}
